import {Router} from 'express'; import {auth} from '../middleware/auth.js'; import {Customer} from '../models/Customer.js'; import {Policy} from '../models/Policy.js'; import {ServiceRequest} from '../models/ServiceRequest.js'; import {User} from '../models/User.js';
const r=Router(); r.get('/',auth,async(_req,res,next)=>{try{const [customers,policies,requests,users,recent,byStatus]=await Promise.all([Customer.countDocuments(),Policy.countDocuments(),ServiceRequest.countDocuments(),User.countDocuments(),ServiceRequest.find().sort({createdAt:-1}).limit(5).populate('customer','firstName lastName'),ServiceRequest.aggregate([{$group:{_id:'$status',count:{$sum:1}}}])]);res.json({customers,policies,requests,users,recent,byStatus});}catch(e){next(e)}});export default r;
