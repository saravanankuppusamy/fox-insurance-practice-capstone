import {useEffect,useState} from 'react';import api from '../services/api';import type{ServiceRequest}from'../types';export default function Requests(){const[x,setX]=useState<ServiceRequest[]>([]);const[status,setStatus]=useState('');const load=()=>api.get('/requests',{params:status?{status}:{}}).then(r=>setX(r.data));useEffect(load,[status]);return <><h2>Service Requests</h2><label>Status <select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option><option>open</option><option>in-progress</option><option>resolved</option><option>closed</option></select></label><table><thead><tr><th>#</th><th>Category</th><th>Description</th><th>Priority</th><th>Status</th></tr></thead><tbody>{x.map(r=><tr key={r._id}><td>{r.requestNumber}</td><td>{r.category}</td><td>{r.description}</td><td>{r.priority}</td><td>{r.status}</td></tr>)}</tbody></table></>}
