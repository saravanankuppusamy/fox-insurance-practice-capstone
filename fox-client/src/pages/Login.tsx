import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Login(){const[email,setEmail]=useState('admin@foxinsurance.demo');const[password,setPassword]=useState('Password123!');const[error,setError]=useState('');const nav=useNavigate();const{login}=useAuth();return <div className="login"><h2>Sign in</h2><form onSubmit={async e=>{e.preventDefault();try{await login(email,password);nav('/')}catch{setError('Login failed')}}}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button>Login</button></form>{error&&<p className="error">{error}</p>}</div>}
